@model IEnumerable<Szakdoli.Models.TermekTipus>

@{
    ViewData["Title"] = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h1>Index</h1>

@if (User.IsInRole("Admin") || User.IsInRole("Raktar Vezeto"))
{
    <p>
        <a asp-action="Create">Create New</a>
    </p>
}

<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.TipusNev)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Suly)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
        @foreach (var item in Model)
        {
            <tr>
                <td>
                    @Html.DisplayFor(modelItem => item.TipusNev)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.Suly)
                </td>
                <td>
                    <a asp-action="Edit" asp-route-id="@item.TipusID">Edit</a> |
                    <a asp-action="Details" asp-route-id="@item.TipusID">Details</a> |
                    <a asp-action="Delete" asp-route-id="@item.TipusID">Delete</a>
                </td>
            </tr>
        }
    </tbody>
</table>
